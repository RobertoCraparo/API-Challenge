# Definizione del tipo di dato GraphQL
type Film {
    id: ID!
    titolo: String!
    regista: String!
    anno: Int!
}

# Definizione delle operazioni di lettura
type Query {
    getFilmsGraphql: [Film]
}